import Index from '@/Index';
import About from '@/About';

const routes = [
  {
    path: '/',
    component: Index,
  },
  {
    path: '/about',
    component: About,
  },
];

export default routes;
